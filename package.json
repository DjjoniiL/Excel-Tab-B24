{
  "name": "excel-tab-b24",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless Bitrix24 deal card tab with an editable spreadsheet-like grid.",
  "scripts": {
    "lint": "node --check app.js && node --check install.js && node --check tests/app.test.js && node --check tools/check-marketplace-files.js",
    "test": "node tests/app.test.js",
    "package": "powershell -ExecutionPolicy Bypass -File tools/build-marketplace-zip.ps1"
  }
}
