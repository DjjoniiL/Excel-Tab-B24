Продолжи работу над проектом Excel Tab B24.

Локальный путь проекта:
G:\AI Project B24\Excel Tab B24

GitHub:
https://github.com/DjjoniiL/Excel-Tab-B24
Remote: origin
Branch: main

Обязательно используй актуальный локальный учебник верхнего уровня:
G:\AI Project B24\Bitrix24 CRM Marketplace App Logic.md

Перед началом работы прочитай документацию проекта:
- NEXT_SESSION.md
- PROJECT_SPECIFICATION.md
- DESIGN_GUIDE.md
- README.md
- RELEASE_REPORT_v19.md

Текущее состояние:
- Проект является serverless-приложением для Bitrix24 Marketplace без backend.
- Runtime-файлы приложения: `install.html`, `install.js`, `install.css`, `index.html`, `app.js`, `style.css`.
- Marketplace zip должен включать только runtime-файлы, без документации, тестов, `.git`, `.env`, `node_modules` и служебных файлов.
- Последняя собранная Marketplace-версия: `G:\AI Project B24\Excel Tab B24\dist app B24 zip\Excel Tab B24 v.19.zip`.
- Последний push в GitHub выполнен в `main`, актуальный верхний коммит после документации v.19: `c50966e`.
- Рабочая папка после последнего push была чистой по `git status --short`.

Что реализовано к v.19:
- Вкладка сделки показывает Excel-подобную таблицу 9x7 по умолчанию, кегль 13 пт.
- Есть режим таблицы текущей сделки и общей таблицы воронки.
- Пользователь может добавлять строки и столбцы.
- Добавление столбцов должно сохранять значения, CRM-привязки и форматирование.
- Скрытая кнопка выбора CRM-поля больше не должна визуально обрезать текст ячеек после добавления столбцов.
- Стартовая видимая область должна показывать заголовок и все 9 базовых строк над горизонтальным скроллом.
- Можно редактировать ячейки, вставлять значения полей сделки, выделять одиночные ячейки, диапазоны через Shift, несколько ячеек через Ctrl, строки, столбцы и всю таблицу.
- Есть перенос текста, автоширина колонок, заливка, жирный, курсив и кегль 11/13/15/18 пт.
- Кегль 13 пт теперь является явным значением селектора и должен применяться к нескольким выделенным ячейкам.
- Кнопка `Удалить` очищает данные, CRM-привязки, заливку, жирный, курсив, кегль и перенос; больше 18 ячеек требуют подтверждения.
- Формулы вида `=E4+B4` поддерживают ссылки на ячейки, числа, скобки и операции `+`, `-`, `*`, `/`.
- После ввода `=` клик по ячейкам добавляет ссылки в формулу.
- Enter сохраняет ввод/формулу и выходит из режима редактирования.
- Есть сохранённые формулы: добавление, удаление, прокрутка списка, применение к выбранной ячейке.
- При вводе `=` под ячейкой появляется список до 5 последних формул.
- Ctrl-выделение от ячейки с формулой протягивает формулу с относительным смещением ссылок.
- Экспорт `.xls` должен выгружать заполненную область таблицы; формульные ячейки экспортируются как формулы Excel, не как текст рассчитанного значения.
- Кнопка `Обновить поля` обновляет CRM-привязанные значения и сжимает текущую таблицу до последней заполненной строки/колонки, но не меньше 9x7.

Права для текущей версии:
- CRM (CRM)
- Placement / Встраивание приложений
- `user_basic`

Для текущей версии НЕ нужно:
- полный `user`
- `user.userfield`

Текущие REST-методы:
- `placement.unbind`
- `placement.bind`
- `crm.deal.fields`
- `crm.deal.get`
- `crm.contact.get`
- `crm.company.get`
- `crm.dealcategory.list`
- `crm.status.list`
- `user.get`

Обязательный цикл каждой итерации:
1. Выполнить `git status --short`.
2. Внести изменения.
3. Выполнить `npm run lint`.
4. Выполнить `npm test`.
5. Перед сборкой Marketplace-версии обновить `PROJECT_SPECIFICATION.md`.
6. Если собирается Marketplace-пакет, выполнить `npm run package` и проверить состав zip.
7. Сделать commit.
8. Перед push спросить разрешение пользователя, если он не дал его явно в текущей итерации.

Ключевые правила проекта:
- Marketplace-версия должна быть serverless, без backend.
- OAuth/API ключи, portal credentials, `.env` и любые секреты не записывать в документацию, код, архивы и репозиторий.
- Для Marketplace создавать только новые versioned zip-архивы, старые архивы не перезаписывать.
- Архивы складывать в папку: `dist app B24 zip`.
- Название архива должно быть в формате: `Excel Tab B24 v.N.zip`.
- Перед каждой загрузкой в Bitrix24 Marketplace обязательно написать пользователю список прав текущей версии.

Что проверить на портале по v.19 перед вторым этапом:
- В карточке отображается именно загруженная v.19, а не старая v.13/v.18.
- После добавления новых столбцов текст в ячейках не обрезается скрытой кнопкой выбора поля.
- Все 9 строк видны в стартовой области таблицы.
- Экспорт `.xls` открывается в Excel, строки и значения видны, формулы остаются формулами.
- `Обновить поля` обновляет CRM-привязанные значения и сжимает таблицу до заполненной области, но не меньше 9 строк и 7 столбцов.
- Кегль 11/13/15/18 пт применяется сразу к нескольким выделенным ячейкам.

Основная задача следующей сессии: этап 2.

Этап 2 нужно делать на базе пересобранного после v.19 zip:
- Добавить выделение нескольких ячеек протягиванием при удержании левой кнопки мыши.
- Добавить ручное изменение ширины столбцов перетаскиванием границы заголовка/столбца.
- Добавить ручное изменение высоты строк перетаскиванием границы строки/ячейки.
- Сохранять пользовательские высоты строк вместе с состоянием листа.
- Проверить, что drag-select и resize не ломают редактирование ячеек, формулы по клику, Ctrl-протяжку формул и выбор CRM-поля.
- После этапа 2 обновить `PROJECT_SPECIFICATION.md`, собрать новый versioned zip, проверить состав zip, сделать commit и спросить/получить разрешение на push.
